{
    "title":"{{page.title}}",
    "authors":[{% for author in page.author %}"{{author}}",{% endfor %} null], 
    "category":"{{page.category}}", 
    "tags":[{% for tag in page.tags %}"{{tag}}",{% endfor %} null],
    "summary":"{{ page.summary | replace:'"','''}}",
    "url":"{{ page.url }}",
    
    {% if page.date == null %}
    "date":"NA"
    {% else %}
    "date":"{{ page.date | date: "%d %B %Y"  }}"
    {% endif %}
}
